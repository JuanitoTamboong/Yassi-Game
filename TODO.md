# TODO

## Completed
- Fix welcome audio / background music start behavior on first user gesture.
- Prevent welcome audio from being stopped instantly on dismiss.
- Reduce bottom menu overlap on mobile by switching to dynamic viewport height units (dvh) and adding safe-area padding.

## Next (if needed)
- If any devices still show overlap: add additional mobile override for `.bottom-menu` and adjust `bottom` offsets for `.character` / `.current-skin`.
- Optional: add `-webkit-backdrop-filter` fallbacks for Safari.

